public class Invalid_Amount_Exception extends Exception {
    // CONSTRUCTOR PASSES ERROR MESSAGE TO PARENT EXCEPTION CLASS
    public Invalid_Amount_Exception(String message) {
        super(message);
    }
}